# === PRIVATE track (dual-track) — 絶対に tracked にしない ===
# internal AI-process / planning / self-audit docs (kept local, never public)
CLAUDE.md
docs/SESSION_HANDOFF.md
docs/specs/
docs/evidence/tier_self_audit_2026_06_30.md
.claude/
.env
.env.*
*.env
.tmp/
notes/private/
# optional secret/term wordlist — never commit
scripts/.private_wordlist.txt
# run manifests (generated output, not source)
runs/
# real datasets downloaded on demand (MovieLens etc.) — never redistributed
data/
# rendered demo video + temp (delivered out-of-band, not in repo)
out_video/
# local video-tooling (narration pipeline), not part of the OSS product
scripts/produce_video.py
.message_display.jsonl

# === Python ===
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.uv/

# === test / cache ===
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# === models / data(重量級・再生成可) ===
.cache/
*.bin
*.safetensors
qdrant_storage/

# === OS ===
.DS_Store
Thumbs.db
